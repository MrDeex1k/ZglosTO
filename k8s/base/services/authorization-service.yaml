apiVersion: v1
kind: Service
metadata:
  name: authorization
  namespace: zglosto
  labels:
    app: authorization
spec:
  selector:
    app: authorization
  ports:
    - name: mtls
      port: 9956
      targetPort: 9956
      protocol: TCP
  type: ClusterIP
  sessionAffinity: None
