{
  "name": "@zglosto/white-label-config",
  "version": "1.0.0",
  "private": true,
  "license": "SEE LICENSE IN ../../LICENSE",
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "bun run --bun tsc -p tsconfig.build.json",
    "metadata": "bun dist/cli.js",
    "test": "bun run --bun vitest run src",
    "test:watch": "bun run --bun vitest src",
    "typecheck": "bun run --bun tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@zglosto/contracts": "workspace:*",
    "yaml": "2.9.1"
  },
  "devDependencies": {
    "@types/node": "26.6.2",
    "typescript": "7.0.2",
    "vitest": "5.0.1"
  }
}
