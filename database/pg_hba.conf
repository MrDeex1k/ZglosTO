# Local socket access is restricted to processes inside the PostgreSQL container.
local all all trust

# Every TCP client must use TLS and SCRAM-SHA-256.
hostnossl all all 0.0.0.0/0 reject
hostnossl all all ::/0 reject
hostssl all all 0.0.0.0/0 scram-sha-256
hostssl all all ::/0 scram-sha-256
