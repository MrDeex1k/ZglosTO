FROM dpage/pgadmin4:latest

# This Dockerfile is a simple reference image for pgAdmin 4.
# No customizations are required because the service uses the official image.
# Keep this file if you want to extend or customize the pgAdmin image later.
