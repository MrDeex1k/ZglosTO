# label

- Źródło: shadcn `base-nova`, CLI `4.14.0`.
- Wrapper pozostaje natywnym `label`.
- Typ lokalny wymaga `htmlFor`, dzięki czemu konsument nie może utworzyć niepowiązanej etykiety.
- OxLint accessibility i React Doctor: OK.
