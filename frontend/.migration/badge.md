# badge

- Źródło: shadcn `base-nova`, CLI `4.14.0`.
- Kompozycja korzysta z `useRender` i `mergeProps` Base UI.
- Zachowano obsługę lokalnego `style` używanego przez kolory służb.
- TypeScript, OxLint, build i React Doctor: OK.
