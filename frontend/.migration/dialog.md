# dialog

- Źródło: shadcn `base-nova`, CLI `4.14.0`.
- Prymityw: `@base-ui/react/dialog`.
- `DialogTrigger asChild` zastąpiono kontraktem `render`.
- Controlled `open`/`onOpenChange`, portal, Escape i powrót fokusu do triggera: OK.
