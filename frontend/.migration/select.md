# select

- Źródło: shadcn `base-nova`, CLI `4.14.0`.
- Prymityw: `@base-ui/react/select`.
- Wszystkie kontrolowane selecty otrzymały jawne `items`; zapobiega to wyświetlaniu klucza domenowego zamiast etykiety.
- Portal, lista, wybór klawiaturą, etykieta wybranej wartości i zamknięcie: OK.
